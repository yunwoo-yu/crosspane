---
"crosspane": patch
---

All user-facing text is now English — dashboard UI (toasts, empty states, tooltips, placeholders, diff/network/console panels) and CLI runtime messages (install prompts, fallback warnings, URL convergence logs). Matches the English README and npm listing.
