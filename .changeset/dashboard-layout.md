---
"crosspane": patch
---

The session panel now fills the window. Since the pane grid was removed in 0.7.0 the layout still reserved space above the panel, so logs were confined to a strip at the bottom while most of the screen sat empty. The now-vestigial drag-to-resize handle is gone with it.
