# 워크스페이스 패키지는 버전이 맞으면 로컬 심볼릭 링크로 해석한다.
# 의존성 명세에 workspace: 프로토콜을 쓰지 않는 이유: npm publish는 이를 치환하지
# 못해 배포본에 그대로 남고, 설치가 EUNSUPPORTEDPROTOCOL로 깨진다 (0.7.0 사고).
link-workspace-packages=true
prefer-workspace-packages=true
